name: Build Rathole for mipsel (static)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install specific nightly (supports edition 2024)
        uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2025-08-01
          components: rust-src

      - name: Build static rathole (client only)
        run: |
          cargo build --release \
            --target mipsel-unknown-linux-musl \
            -Z build-std=std,core,alloc \
            --no-default-features \
            --features "client"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: rathole-mipsel-static
          path: target/mipsel-unknown-linux-musl/release/rathole
